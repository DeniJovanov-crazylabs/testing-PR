name: Print GitHub Ref

on:
  pull_request:
    types: [closed]

jobs:
 print-ref:
   runs-on: self-hosted
   steps:
     - name: Checkout code
       uses: actions/checkout@v2

     - name: Print GitHub Ref
       run: echo "${{ github.head.ref }}"
